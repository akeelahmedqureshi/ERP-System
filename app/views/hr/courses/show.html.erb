<h1 class = "text-center">course</h1>
<table class="table">
  <thead>
    <tr>
      <th scope="col">Title</th>
      <th scope="col">Description</th>
      <th scope="col">Price</th>
      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @course.title %></td>
      <td><%= @course.description %></td>
      <td><%= @course.price %></td>
      <td><%= button_to "Delete", hr_course_path(@course.id), method: :delete, class: "btn btn-secondary" %></td>
    </tr>
  </tbody>
</table>